<% reservation = Reservation.new %>
<%= form_for(reservation) do |f| %>

  <% if reservation.errors.any? %>
	<div id="error_explanation">
      <h2><%= pluralize(reservation.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% reservation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= hidden_field_tag 'timeslot[restaurant_id]', @restaurant.id %>

  <div class="field">
    <%= f.label "Test" %><br />
    <%= select_tag 'timeslot[start_time]', options_from_collection_for_select(@restaurant.generate_two_weeks_of_datetimes(@restaurant), :last, :first ) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>